"use strict";
// nodemon automatically restarts the node application when file changes in the directory are detected.
// nodemon is a replacement wrapper for node, to use nodemon replace the word node on the command line when executing your script.

// normal run command: node xxx.js
// modenon run command: nodemon xxx.js, it will not stop after the command because it detects the changes,
// if you want to stop the app, just ctrl+C
console.log("Hello, Nodemon!");